{
  "name": "@buggyorg/library-cli",
  "version": "0.1.0",
  "description": "CLI interface for library changes.",
  "main": "lib/cli.js",
  "scripts": {
    "test": "standard src/**/*.js && mocha --compilers js:babel-register",
    "build": "babel src -d lib"
  },
  "author": "Maximilian Klein",
  "license": "MIT",
  "dependencies": {
    "@buggyorg/library-client": "^0.4.1",
    "yargs": "^6.4.0"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0",
    "babel-register": "^6.18.0",
    "chai": "^3.5.0",
    "mocha": "^3.1.2",
    "standard": "^8.5.0"
  }
}
